{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "install-frontend": "cd frontend && npm install --legacy-peer-deps && vite build",
    "setup-production": "npm run install-frontend",
    "preview": "vite preview",
    "start": "cd backend && npm install && npm run server"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "@vitejs/plugin-react": "^3.1.0",
    "eslint": "^8.41.0",
    "eslint-plugin-flowtype": "^8.0.3",
    "https": "^1.0.0",
    "vite": "^4.1.0"
  },
  "proxy": "http://localhost:4000",
  "allowedHosts": "all"
}

