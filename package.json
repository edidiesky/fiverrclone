{
  "scripts": {
    "dev": "vite",
    "build": "npm i vite @vitejs/plugin-react@3.1.0 && cd frontend && npm install --legacy-peer-deps && vite build",
    "start": "cd backend && npm install && npm run server",
    "preview": "vite preview"
  },
  "allowedHosts": "all",
  "dependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.4.9"
  }
}
