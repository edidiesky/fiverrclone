import React from "react";
import { CustomizedCheckbox } from "../../forms";
export default function FilterTypes() {
  if (type === "seller") {
    return <div>FilterTypes</div>;
  }
  if (type === "budget") {
    return <div>FilterTypes</div>;
  }
  if (type === "time") {
    return <div>FilterTypes</div>;
  }
  return <div>FilterTypes</div>;
}
